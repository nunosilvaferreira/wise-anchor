import AddTaskForm from "../../components/add-task-form";

export const metadata = {
  title: "Add Task | WiseAnchor",
  description: "Add a new routine step to your WiseAnchor plan.",
};

export default function AddTaskPage() {
  return <AddTaskForm />;
}
