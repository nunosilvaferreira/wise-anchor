import LoginForm from "../../components/login-form";

export const metadata = {
  title: "Login | WiseAnchor",
  description: "Sign in to sync routines and caregiver profiles with Firebase.",
};

export default function LoginPage() {
  return <LoginForm />;
}
