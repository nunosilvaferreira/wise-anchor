import RegisterForm from "../../components/register-form";

export const metadata = {
  title: "Register | WiseAnchor",
  description: "Create caregiver or independent user accounts for WiseAnchor.",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
